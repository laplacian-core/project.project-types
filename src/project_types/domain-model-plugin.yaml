project_types:
- name: domain-model-plugin
  description: Project for a schema model expressed by the `metamodel` model
  base_type_name: plugin
  models:
  - group: laplacian
    name: metamodel
    version: '1.0.0'
  templates:
  - group: laplacian
    name: domain-model-plugin.project-template
    version: '1.0.0'
