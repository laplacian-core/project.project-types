project_types:
- name: schema-model
  description: Project for a schema model expressed by the `metamodel` model
  base_type_name: model
  plugins:
  - group: laplacian
    name: metamodel-plugin
    version: '1.0.0'
  models:
  - group: laplacian
    name: metamodel
    version: '1.0.0'
  templates:
  - group: laplacian
    name: schema.document-template
    version: '1.0.0'




